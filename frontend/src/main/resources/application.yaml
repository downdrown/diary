spring:
  application:
    name: Diary
management:
  metrics:
    export:
      influx:
        enabled: ${INFLUX_ENABLED:false}
        uri: ${INFLUX_URI:}
        org: ${INFLUX_ORGANIZATION:}
        bucket: ${INFLUX_BUCKET:}
        token: ${INFLUX_TOKEN:}